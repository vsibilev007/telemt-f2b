# /etc/fail2ban/action.d/ufw-telemt.conf
# Бан через UFW — блокирует все входящие с IP

[Definition]
actionban   = ufw insert 1 deny from <ip> to any comment "f2b-telemt"
actionunban = ufw delete deny from <ip> to any

[Init]
